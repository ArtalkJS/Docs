# 🧪 构建 · 前端

```sh
$ git clone https://github.com/ArtalkJS/Artalk.git

$ cd Artalk

$ yarn build
```

编译文件将会被输出到 `dist` 目录

## 发布到 NPM

```sh
$ yarn publish
```
